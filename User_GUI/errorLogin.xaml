<Window x:Class="User_GUI.errorLogin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:User_GUI"
        mc:Ignorable="d"
        Title="errorLogin" Height="150" Width="300">
    <Grid Background="Red">
        <Grid.RowDefinitions>
            <RowDefinition>
            </RowDefinition>
            <RowDefinition>
            </RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            There is no such user or password is incorrect
        </TextBlock>
        <Button Name ="exitErrorDifPass" Click="exitErrorButtonClicked" Grid.Row="1" Margin="50,20,50,15">
            Close
        </Button>
    </Grid>
</Window>
